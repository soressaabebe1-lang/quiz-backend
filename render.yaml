services:
  - type: web
    name: S_quiz
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python make_admin.py soressa && gunicorn app:app"
    envVars:
      - key: JWT_SECRET_KEY
        generateValue: true
      - key: ADMIN_SETUP_KEY
        sync: false